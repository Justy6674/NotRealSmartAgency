# /design-shotgun

Design shotgun: generate multiple AI design variants, open a comparison board, collect structured feedback, and iterate.

## What to do

Invoke the **`design-shotgun`** skill with the Skill tool, passing anything the user
typed after the command as its arguments. Follow that skill's instructions from
its first step; do not summarise, shortcut, or re-implement them here.

## When this applies

Standalone design exploration you can run anytime. Use when: "explore designs", "show me options", "design variants", "visual brainstorm", or "I don't like how this looks". Proactively suggest when the user describes a UI feature but hasn't seen what it could look like.

---
Wrapper only. The skill itself lives at `~/.claude/skills/gstack/design-shotgun/SKILL.md`
and is the single source of truth — this file is regenerated, never hand-edited.
